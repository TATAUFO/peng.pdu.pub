# 如果未指定源，则默认为 https://rubygems.org/
source "https://rubygems.org"

# 使用 GitHub Pages 的官方 gem 来保证兼容性
gem "github-pages", group: :jekyll_plugins

# 如果不使用 GitHub Pages，可以使用 Jekyll 的标准 gem
# gem "jekyll", "~> 4.3.2" # 根据需要调整版本号

# 如果需要 Sass 进行样式预处理
# gem "jekyll-sass-converter"

# 如果使用了分页功能
# gem "jekyll-paginate", "~> 1.1"

# 如果需要 SEO 优化
# gem "jekyll-seo-tag", "~> 2.7"

# 如果需要生成站点地图
# gem "jekyll-sitemap", "~> 1.4"

# 如果使用 Liquid 模板语言中的高亮代码块
# gem "rouge", "~> 4.0"

# 如果需要 Jekyll 的多语言支持插件
# gem "jekyll-multiple-languages-plugin", "~> 1.8"

# 如果需要支持 RSS feed
# gem "jekyll-feed", "~> 0.16"

# 如果你需要 LiveReload 支持以进行本地实时预览（开发环境专用）
# gem "jekyll-livereload", group: :jekyll_plugins

# 其他你可能需要的插件
# gem "jekyll-optional-plugin"
